1. this directory saves my personal setting for '.bashrc' and '.profile'

2. some script neet auto start at system reboot or after user login,
   those scrpit is saved in ~/ShellScripts/basic_scripts/ directory,
   it's need use follw command to install :
        $ source ~/ShellScripts/login-auto-start/install
